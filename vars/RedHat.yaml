---
# RHEL 6 (Old)
#patching_journal: "yum history info $(yum history list | awk '{ print $1 }' | head -n 5 | tail -n 1)"

# RHEL 7 (Old)
#patching_journal: "journalctl -b -t 'yum'"

patching_journal: "rpm -qa --last | grep {{ lookup('pipe','date +\"%a %d %b %Y\"') | quote }} | awk '{ print $1 }'"